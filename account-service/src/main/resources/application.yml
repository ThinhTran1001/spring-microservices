server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/account_service"
    username: admin1
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
